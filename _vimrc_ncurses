" COLORS
colorscheme herald 			" set colorscheme
syntax enable


" UI CONFIG
set nocompatible
set number					" show line numbers
set cursorline				" highlight current line
set showcmd

set visualbell

set wildmenu

filetype on
filetype indent on
filetype plugin on

set showmatch				"highlight matching parenths


"STATUSLINE
"COLORS
hi User1 ctermfg=172 guifg=#eea040 ctermbg=8 guibg=#222222
hi User2 ctermfg=9 guifg=#dd3333 ctermbg=8 guibg=#222222
hi User3 ctermfg=7 guifg=#bbbbbb ctermbg=8 guibg=#222222
hi User4 ctermfg=7 guifg=#aaaaaa ctermbg=8 guibg=#222222
hi User5 ctermfg=15 guifg=#ffffff ctermbg=8 guibg=#222222


" ALWAYS SHOWING
set laststatus=2

set statusline=
set statusline +=%5*\[%n]    " buffer number
set statusline +=%1*\ %<%F\  " File+path
set statusline +=%2*%m%*    " modified flag
set statusline +=%2*\ %y\    " FileType
set statusline +=%3*%=[
set statusline +=%4*%=line:
set statusline +=%1*%=%l%*             "current line
set statusline +=%2*%=/
set statusline +=%2*%=%L%*               "total lines
set statusline +=%3*%=]

" SPACES AND TABS
set autoindent
set tabstop=4
set softtabstop=4
set expandtab


" SEARCHING
set incsearch
set hlsearch

" FOLDING
set foldenable
set foldlevelstart=10
set foldnestmax=10
nnoremap <space> za
"SPACE OPENS and CLOSES folds

" LEADER SHORTCUTS
let mapleader=";"

" REMAP ESCAPE
imap jk <Esc>
vmap jk <Esc>

" REMAP SAVE
imap <leader>ss <Esc>:w!<CR>

" TURN OFF SEARCH HIGHLIGHT
nmap <leader><space> :nohlsearch<CR>
nmap <silent> <BS> :nohlsearch<CR>

" PERSONAL ZEN-CODING
imap <leader>s <style type="text/css"><CR></script><Esc>O
imap <leader>js <script type="text/javascript"><CR></script><Esc>O<TAB>
imap <leader>jf function a() {<CR>}<Esc>O<TAB>var = 
imap <leader>tag getElementByTagName
imap <leader>id getElementById
imap <leader>i .innerHTML

" PERSONAL ABBREVIATIONS
iabbrev )) ()
iabbrev chadd@ chadd@7thMetric.com
iabbrev ture true
iabbrev flase false
iabbrev adn and
iabbrev teh the
iabbrev copyright © 2016 All rights reserved.

nnoremap / /\v 
" CUSTOM FUNCTIONS
" toggle between number and relativenumber
function! ToggleNumber()
    if(&relativenumber == 1)
        set norelativenumber
        set number
    else
        set relativenumber
    endif
endfunc

" strips trailing whitespace at the end of files. this
" is called on buffer write in the autogroup above.
function! <SID>StripTrailingWhitespaces()
    " save last search & cursor position
    let _s=@/
    let l = line(".")
    let c = col(".")
    %s/\s\+$//e
    let @/=_s
    call cursor(l, c)
endfunction


